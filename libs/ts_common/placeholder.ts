// libs/ts_common/placeholder.ts
// This directory is intended for shared TypeScript types and utilities.
// It will be managed and generated via Turbo/Nx as per folderstructure.md.

export interface ExampleType {
  id: string;
  name: string;
} 